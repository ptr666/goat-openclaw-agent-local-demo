[profile.default]
src = "src"
out = "out"
script = "script"
libs = ["lib"]
solc_version = "0.8.24"
evm_version = "paris"
optimizer = true
optimizer_runs = 200
fs_permissions = [{ access = "read-write", path = "./" }]

[rpc_endpoints]
goat_testnet3 = "${GOAT_RPC_URL}"
